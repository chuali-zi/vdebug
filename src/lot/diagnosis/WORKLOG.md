本次补全了 diagnosis 模块的核心闭环：`facts.py` 现在会从公开事件与稳定 runtime 上下文中提取 `bus_stuck_low`、`repeated_nack`、`gpio_direction_conflict`、`uart_baud_mismatch` 四类事实，并保留 `source_events` 证据链；`rules.py` 增加了内置规则表；`explainer.py` 改为基于事实生成稳定 hypothesis、confidence、observations、next_actions 与 uncertainty_note；`service.py` 接入 runtime 以支持历史事件窗口聚合。已用 `.venv\Scripts\python.exe` 跑过 `tests.test_api/tests.test_engine/tests.test_devices`，均通过，并做了定向烟测。注意：当前 engine 只向 diagnosis 传归一化事件，未透传 devices 原始子事件，因此部分事实需从 `payload.result` 反推，若后续要更强证据链，应在引擎接缝补原始事件透传，但本次按要求未跨模块修改。
